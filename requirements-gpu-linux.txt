# Optional GPU-only dependencies. Install this file only on Linux GPU environments.
# Example:
# pip install -r requirements.txt -r requirements-gpu-linux.txt

triton>=3.3,<3.4; platform_system == "Linux" and platform_machine == "x86_64"
# TensorRT 11 uses strongly typed networks; install a release supported by the target GPU.
tensorrt-cu12>=11.2,<12; platform_system == "Linux" and platform_machine == "x86_64"
