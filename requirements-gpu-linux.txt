# Optional GPU-only dependencies. Install this file only on Linux GPU environments.
# Example:
# pip install -r requirements.txt -r requirements-gpu-linux.txt

triton>=3.2,<3.3; platform_system == "Linux" and platform_machine == "x86_64"
tensorrt-cu12>10.0,<11; platform_system == "Linux" and platform_machine == "x86_64"
